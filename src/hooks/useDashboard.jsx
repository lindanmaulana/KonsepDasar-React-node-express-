
const useDashboard = (props) => {
    const {headerState, sideBarState} = props
    console.log({headerState: headerState, sideBarState: sideBarState})
  return (
    <div>
      
    </div>
  )
}

export default useDashboard
